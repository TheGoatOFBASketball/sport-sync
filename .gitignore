.vercel

# Generated by `node scripts/copy-public.js` at Vercel build time.
# Don't commit the local copy — Vercel rebuilds it on every deploy, and the
# per-machine mtimes would otherwise dirty the working tree.
public/
