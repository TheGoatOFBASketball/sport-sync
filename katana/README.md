Katana integration for SportSync

- This folder contains a minimal config and scaffolding to run GitHub Katana against basketball sites.
- It is a placeholder; you should replace example URLs with sites you have rights to scrape and configure endpoints for the data you want (live-scores, standings, news, schedule).
- The server-side proxy will call Katana and expose a REST API for the frontend to consume.
